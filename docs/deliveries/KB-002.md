# KB-002

## Goal

Add the first architecture reference and fix solution membership for local restore.

## Scope

- Include `Genkidama.Blueprints` in `Genkidama.slnx`.
- Add architecture index.
- Add solution composition reference.
- Update Knowledge Base index.

## Local issue addressed

Visual Studio can report restore error NU1105 when a referenced project is not loaded through the solution. `Genkidama.Cli.Tests` references `Genkidama.Blueprints`, so `Genkidama.Blueprints` must be listed in `Genkidama.slnx`.

## Branch note

The active branch uses book naming: `job-kb002`.
