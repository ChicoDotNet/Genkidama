# GEN-022

## Goal

Close the first Genkidama pod by preparing the CLI package.

## Scope

- Configure `Genkidama.Cli` package metadata.
- Set the command name to `genkidama`.
- Add CI package validation.
- Add package metadata tests.
- Add tool notes.

## Acceptance criteria

- The CLI project is packable.
- The command name is stable.
- CI validates package creation.
- Publishing remains out of scope.
- Existing CLI commands remain unchanged.

## Dev note

This branch was created directly from the current `dev` branch.
